{
  "name": "cookbooksocial",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "cd backend && npm install && node index.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^9.16.0"
  }
}
